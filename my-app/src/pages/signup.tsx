import Format from "@/components/format";

export default function SignUp() {
    
    return (
      <Format body={
        <div>
            <p>Sign Up page</p>                
        </div>

        }/>
    );
}
