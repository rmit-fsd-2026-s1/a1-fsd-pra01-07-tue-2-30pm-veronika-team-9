import Format from "@/components/format";

export default function Guest() {
    return(
        <Format body={
            <div>
              <p>Non-logged in placeholder</p>      
            </div>
        }/>
    );
}
